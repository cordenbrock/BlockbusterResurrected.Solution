@{
  Layout = "_Layout";
}

@using BlockbusterResurrected.Models;
@model List<BlockbusterResurrected.Models.GConsole>;

<h1>Consoles</h1>
<hr>

@if (@Model.Count == 0)
{
  <p>No Consoles have been listed yet</p>
}

@foreach (GConsole console in Model)
{
  <p>@Html.ActionLink($"{console.GConsoleName}", "Details", new { id = console.GConsoleId })</p>
}
<hr>

<p>@Html.ActionLink("Add new console", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>